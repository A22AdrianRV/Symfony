{% extends 'base.html.twig' %}

{% block title %}
    Create a record. | {{ parent() }}
{% endblock %}

{% block body %}
<h1>{{title}}</h1>

<div>
    Tracks
    <ul>
        {% for track in tracks %}
            <li>
                {{track.artist|upper}}- {{track.song}}
            </li>
        {% endfor %}
    </ul>
<div>
{% endblock %}